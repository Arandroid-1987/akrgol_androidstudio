<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="60dip"
    android:padding="5dip">

    <ImageView
        android:id="@+id/notiziaImage"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        android:contentDescription="@string/app_name">

    </ImageView>

    <com.arandroid.risultatilive.ui.ScrollingTextView
        android:id="@+id/notiziaTitle"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_gravity="bottom"
        android:layout_marginLeft="14dp"
        android:layout_marginStart="1dp"
        android:layout_toLeftOf="@+id/notiziaAuthor"
        android:layout_toStartOf="@id/notiziaAuthor"
        android:layout_toRightOf="@+id/notiziaImage"
        android:layout_toEndOf="@+id/notiziaImage"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:padding="5dip"
        android:paddingBottom="15dip"
        android:paddingTop="15dip"
        android:scrollHorizontally="true"
        android:singleLine="true"
        android:text="@string/app_name"
        android:textSize="15sp" />

    <TextView
        android:id="@+id/notiziaAuthor"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/notiziaTitle"
        android:layout_alignBottom="@+id/notiziaTitle"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_marginRight="13dp"
        android:layout_marginEnd="13dp"
        android:text="@string/app_name"
        android:textSize="15sp" />

</RelativeLayout>